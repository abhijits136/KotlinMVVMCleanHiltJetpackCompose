package com.example.kotlinmvvmcleanhiltjetpackcompose.data.model

/**
 * Represents a raw data item as it might be received from a data source (e.g., network or database).
 * This entity is internal to the data layer.
 */
data class ShowcaseItemEntity(
    val itemName: String
)
